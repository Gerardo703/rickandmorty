export const Footer = ({title}) => {
    return ( 
        <>  
            {/* Footer */}
            <div>
                <nav className="footer">
                    {title}
                </nav>
            </div>
        </>
    );
}
